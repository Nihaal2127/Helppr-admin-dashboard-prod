/** @deprecated Import from `utility` — kept for stable path during migration. */
export type { RequestApprovalStatus } from "./utility";
export {
  formatRequestedBy,
  isCatalogRequestRow,
  mapApprovalStatusFromRecord,
  mergeCategoryDetailForDialog,
  mergeServiceDetailForDialog,
  requestApprovalStatusColor,
  requestApprovalStatusLabel,
} from "./utility";
