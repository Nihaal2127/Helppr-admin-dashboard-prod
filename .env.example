# Copy to .env for faster `npm run start` on this large CRA app.
DISABLE_ESLINT_PLUGIN=true
GENERATE_SOURCEMAP=false
BROWSER=none
# Chat Service VPS (REST + Socket.IO). Must be HTTPS when the web app is on https://app.helppr.in.
REACT_APP_CHAT_SERVICE_URL=https://chat.helppr.in
# Local/dev direct IP example (HTTP only — do not use on production builds):
# REACT_APP_CHAT_SERVICE_URL=http://13.201.79.72

# If you see "Something is already running on port 3000", run: npm run start:clean
